<div class ="container">
  <div class = "row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
    <h1>Beaches</h1>
	  <ul>
      <% @beaches.each do |beach| %>
        <li><%= beach.name %></li>
        <%= link_to beach.name, beach_path(beach.id) %>
      <% end %>
  </ul>
<%= link_to "Add a new ", new_beach_path %>
    </div>
  </div>
</div>
